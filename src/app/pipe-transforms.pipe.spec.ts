import { PipeTransformsPipe } from './pipe-transforms.pipe';

describe('PipeTransformsPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTransformsPipe();
    expect(pipe).toBeTruthy();
  });
});
